// Edit this OR set via UI Settings (preferred).
// The UI stores Settings in localStorage under "BVMI_API_BASE_URL".
window.APP_CONFIG = window.APP_CONFIG || {
  apiBaseUrl: "" // e.g., "https://abc123.execute-api.ap-south-1.amazonaws.com"
};
